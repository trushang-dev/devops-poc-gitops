apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devops-poc
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
